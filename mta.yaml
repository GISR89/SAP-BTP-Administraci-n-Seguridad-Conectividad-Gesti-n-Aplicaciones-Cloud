_schema-version: 3.3.0
ID: projectcap2
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: projectcap2-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
      memory: 256MB
      disk-quota: 256MB
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: projectcap2-auth
      - name: projectcap2-db

  - name: projectcap2-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: projectcap2-db

resources:
  - name: projectcap2-auth
    type: org.cloudfoundry.managed-service
    parameters:
      memory: 256MB
      disk-quota: 256MB
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: projectcap2-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
  - name: projectcap2-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
